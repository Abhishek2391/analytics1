#the variable should be numeric, so need to convert from categorical to numeric. for linear regression. The target variable should be a continuous. These are pre requisites for linear regression. It is applicable to identify sales of an outlet, or revenue of company, order received .

#incase of target variable is a binomial, two output (1 or 0, Yes or No), we have to build a logistic regression model. Gives probability of the event happening . Pre requisite is when target variable has two categories.Not applicable for more than 2 classes. In that case, we used decision tree.

